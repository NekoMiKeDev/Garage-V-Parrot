{% extends 'base.html.twig' %}

{% block title %}Garage V.Parrot - Formulaire de contact
{% endblock %}

{% block body %}
	<div class="css-dark d-flex justify-content-center minWindowsWidth">
		<button class="btn-back" onclick="goBack()">
			<i class="fa-solid fa-arrow-left"></i>
		</button>
		

			{{ form_start(form, {'attr': {'class': 'b-gold p-4 rounded-3 d-flex flex-column col-12 col-md-6 minWindowsWidth'}}) }}
			<h1 class="h1-gold">Contact</h1>

				
			{{ form_end(form) }}
	</div>



	<script src="{{ asset('scripts/contact/setObjetContact.js') }}"></script>
	{# Set objet to contact #}
{% endblock %}
